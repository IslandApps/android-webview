<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        We inherit from Theme.Material3 (which is AppCompat-compatible)
        to fix the "You need to use a Theme.AppCompat theme" crash.
    -->
    <style name="Theme.WebviewPixel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- The SplashScreen library handles Android 12+ attributes automatically
             using the theme defined in values/themes.xml -->
    </style>
</resources>
